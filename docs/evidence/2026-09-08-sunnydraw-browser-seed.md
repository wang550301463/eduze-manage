# SunnyDraw 浏览器初始化与验收记录

日期：2026-09-08（Asia/Shanghai）。环境：https://www.sunnydraw.com，单台 ECS 测试环境。

本次按用户授权，通过浏览器页面创建三位真实老师及明确标注“验收”的业务数据。未使用 SQL、种子脚本或直接调用业务 API 初始化数据。未向真实老师或家长发送消息。

## 初始化数据

- 总校区任课老师：刘宁（liuning）、蔡依（caiyi）、陈琪（chenqi）。三位账号均已启用并实际登录验证，密码单独保存在本机受限文件中，不进入仓库。
- 课程：验收 · 少儿创意美术（90分钟）。
- 固定班：验收 · 刘宁创意绘画班（周二 09:00–10:30）、验收 · 蔡依色彩探索班（周三 09:00–10:30）、验收 · 陈琪艺术启蒙班（周六 14:00–15:30）。每班容量 8 人，已加入 2 名验收学员；未虚构实际教室。
- 学员：验收小禾、验收小雨（刘宁班），验收小星、验收小月（蔡依班），验收小山、验收小海（陈琪班）。编号依次为 YS2026090801–YS2026090806。初始每人 10 课时，未填写真实家庭联系方式或绑定监护人。
- 使用页面批量生成从 2026-09-07 当周开始的 4 周课表。已查看本周三位老师课次及每课 2 人名单，并在主题中选择刘宁班后续两次课；未独立逐条核对全部四周课次数量。
- 教学模板“验收 · 我的几何花园”已发布固定版本 v1，预计 3 课次，包含年龄、目标、材料及分课次步骤。
- 班级主题“验收 · 刘宁班 · 我的几何花园”采用上述版本，关联 9 月 8、15、22 日实际课次，状态为进行中。
- 学期计划“验收 · 2026秋季创意美术计划”已创建，日期为 2026-09-08 至 2026-12-31；创建时尚无班级主题，因此当前关联主题数为 0。本次未验证事后补充关联。

## 浏览器验证结果

- 三位老师分别登录后，工作台均显示本周 1 节有效课次；刘宁今日课次显示 2 名学生。老师菜单未显示账号、角色及商城管理入口。仅验证菜单表现，未将其视为全部后端权限用例通过。
- 小禾在 9 月 8 日刘宁班签到，按钮变为离园，余额从 10 降至 9，流水显示一笔出勤扣减 90 分钟。未执行并发或重复请求测试，也未完成接走操作。
- 小雨同日请假，原因明确标注验收用途；已批准列表显示该记录，余额仍为 10。
- 小禾主题记录上传两张合成几何花园图片，分别标记过程、成品；页面成功完成真实私有 OSS 上传确认，预览中图片可显示。素材为功能验证图片，不是真实学生作品。
- 小禾记录填写课堂说明和个别点评，进度设为已完成；通过家庭课效预览后点击发布，页面显示“已完成 · 已发布”，草稿云端版本为 4。该版本号不等同于发布版本编号。
- 小雨独立记录保存为待补课，云端版本为 2，保留未发布草稿，无成品。刘宁工作台显示等待补课完成 1 人，不妨碍小禾记录发布。
- 发布后通过刘宁账号再次查看学员余额：小禾仍为 9，小雨及其余四位学员仍为 10，作品发布未产生额外课时扣减。
- 发生并发编辑时页面返回版本冲突，没有静默覆盖；刷新云端记录后分别保存成功。本次未据此宣称整班批量编辑全部场景通过。
- 验收结束恢复管理员登录，浏览器停留在本次班级主题课效页面。

## 本次修复及交付验证

- 旧 API 和教学平台请求遇到会话过期时复用现有刷新队列，最多重新发送一次，避免表单保存或上传因缺少续期处理而中断。
- 作品图片、课件、音频上传用途分别统一为服务端接受的 ARTWORK、COURSEWARE、AUDIO，并增加共享类型约束，修复“上传用途无效”。小程序调用同步修正，但尚未发布或真机验证。
- 请假审批用页面确认弹窗替换浏览器阻塞确认框，保留取消和明确确认操作。
- 学员向导修复末次“下一步”意外触发表单提交的问题，并更新分班提示。真实浏览器验证到达最后一步后仍等待手动提交。
- 前端测试 39 个文件、118 项通过；ESLint 0 错误、4 条已有警告；PC 两个 TypeScript 配置及小程序 TypeScript 检查通过；Vite 生产构建通过；git diff --check 通过。
- 构建 Linux amd64 前端镜像 eduze-web:20260908-ui-validation-3，核验上传归档校验和、镜像 ID 和架构后部署。仅替换 web 容器，未重建数据库或更新其他业务服务。
- 最后检查 Compose 的 12 个容器均为 healthy，包括 8 个业务服务、MySQL、Redis、web 和 gateway。单机健康检查不代表高可用或压测验收。

## 尚未通过或未覆盖

- 补课完整闭环未通过：PC 能创建类型为补课的特殊课，但课次详情未提供添加补课学员的入口，无法通过页面把请假学员加入补课名单。因此本次只验证待补课记录，未宣称补课排入名单、补课签到和完成闭环通过。
- 未绑定真实家庭，课效发布状态不代表家长实际接收、查看或已读。微信 AppID、模板等正式配置及开发者工具、iOS/Android 真机验证仍待完成。
- 老师学员列表可见本校区 6 名学员，未验证按任课老师收窄学员范围；跨校区、其他家庭、撤权及直接媒体访问的完整权限矩阵未在本次执行。
- 未覆盖主题实际延长至第四课次、同模板双班独立推进、合作作品、撤回重发、音视频/PDF 上传、营销及商城支付退款。
- 域名备案办理中，HTTP 入口仍受阿里云备案拦截；本次浏览器使用 HTTPS。不得将本记录等同于正式上线全部验收通过。

结论：初始化已完成，排课、签到扣课、请假、主题记录、真实 OSS 图片上传、预览发布及发布后余额检查通过；补课名单入口及家庭小程序完整闭环仍需后续实现或验收。
