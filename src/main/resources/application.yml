server:
  port: 8080

spring:
  application:
    name: eduze-manage
  profiles:
    active: dev
  web:
    resources:
      add-mappings: false

management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus
  endpoint:
    health:
      show-details: never
