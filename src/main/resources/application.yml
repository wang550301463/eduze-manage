server:
  port: 8080

spring:
  application:
    name: eduze-manage

management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus
  endpoint:
    health:
      show-details: never
